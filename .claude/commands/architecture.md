Switch to ARCHITECTURE phase: $ARGUMENTS

Steps:
1. Run: echo "architecture" > .claude/sdd-layer
2. Read component-boundaries.md, data-models/expression-tree.md, and data-models/index-entities.md.
3. Only create or modify files in doc/architecture/
4. Describe how — pipelines, data flows, component responsibilities, boundary contracts.
5. Cross-check all entity names against data-models/ before finalizing.
6. When done, tell the user to invoke /specification or /free
