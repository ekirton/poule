Switch to TASKS phase: $ARGUMENTS

Steps:
1. Run: echo "tasks" > .claude/sdd-layer
2. Read the specification being decomposed and its parent architecture document.
3. Only create or modify files in tasks/
4. Scope each task to a single implementable unit (file, module, or function).
5. Trace every task to a specific requirement or acceptance criterion.
6. When done, tell the user to invoke /tests or /free
