Switch to REQUIREMENTS phase: $ARGUMENTS

Steps:
1. Run: echo "requirements" > .claude/sdd-layer
2. Read the relevant PRD before editing.
3. Only create or modify files in doc/requirements/
4. Write in business domain language, no technical details.
5. Classify requirements by priority: P0 (must-have), P1 (should-have), P2+ (nice-to-have).
6. When done, tell the user to invoke /features or /free
