Switch to FREE phase (no layer restrictions): $ARGUMENTS

Steps:
1. Run: echo "free" > .claude/sdd-layer
2. All directories are now editable.
3. The upstream-immutability constraint is not enforced in this phase.
4. Use a specific phase command (/requirements, /features, /architecture, /specification, /tasks, /implementation) to re-enable enforcement.
